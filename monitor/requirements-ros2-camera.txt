numpy>=1.26
Pillow>=10.0
httpx>=0.27
python-dotenv>=1.0
websockets>=12.0

# rclpy, sensor_msgs, cv_bridge, and OpenCV come from the robot's ROS 2
# installation. Create the virtualenv with --system-site-packages so these
# binary packages remain ABI-compatible with the vendor camera stack.
